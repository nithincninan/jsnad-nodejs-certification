npm install

  ```
    - Installs all dependencies listed in package.json under:
       "dependencies"
       "devDependencies"
    - Creates a node_modules/ directory (if not already there).
    - Uses package-lock.json if present to install exact versions.
  ```